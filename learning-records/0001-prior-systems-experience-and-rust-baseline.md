# Prior systems experience and Rust baseline

The user has deep C-oriented systems experience across Linux kernel, Android platform, performance/power/thermal profiling, NNStreamer, device drivers, and Android MLOps, but is beginner-to-intermediate in Rust despite completing TRPL, Rustlings, and Rustify. Future lessons should skip generic beginner pacing and instead translate Rust concepts into systems-design consequences, with special attention to closures and concurrency.

**Evidence**

The user described their background and explicitly named closures and concurrency as weak areas.

**Implications**

Start from realistic project pressure and use TDD tasks; explain Rust as a way to encode aliasing, ownership, state transitions, and concurrency invariants rather than as isolated syntax.
