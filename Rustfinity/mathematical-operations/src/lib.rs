pub fn math_operations(a: i32, b: i32) -> (i32, i32, i32, i32) {
    let sum = a + b;
    let difference = a - b;
    let multiply = a * b;
    let divide = a / b;

    (sum, difference, multiply, divide)
}
